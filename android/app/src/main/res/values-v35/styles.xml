<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Android 15 (API 35) enforces edge-to-edge and deprecated Window.setStatusBarColor /
        Window.setNavigationBarColor. These attributes disable the automatic contrast
        enforcement that causes the system to call those deprecated APIs internally.
        The WebView handles safe-area padding via env(safe-area-inset-*) in CSS.
    -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
